mixin head(title, style)
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href=`${style}.scss`)
        title #{title}


mixin header(home, other)
    header
        .menu-btn
            .btn-line
            .btn-line
            .btn-line

        nav.menu
            .menu-branding
                .portrait

            ul.menu-nav
                li(class="nav-item current")
                    a(href=`${home}index.pug`, class="nav-link") Home
                li(class="nav-item current")
                    a(href=`${other}about.pug`, class="nav-link") About me
                li(class="nav-item current")
                    a(href=`${other}work.pug`, class="nav-link") My work
                li(class="nav-item current")
                    a(href=`${other}contact.pug`, class="nav-link") How to reach me
mixin project()
     .item
        a(href="#")
            img(src="", alt="project")
        a(href="#", class="btn-light")
            i(class="fas fa-eye") Project
        a(href="#", class="btn-dark")
            i(class="fab fa-github") Github
